server:
  port: 8087
  servlet:
    context-path: /marketplace

spring:
  application:
    name: marketplace

feign:
  client:
    config:
      default:
        errorDecoder: com.example.marketplaceclient.errorDecoder.MarketplaceErrorDecoder