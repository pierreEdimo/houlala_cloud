server:
  port: 8081
  servlet:
    context-path: /api

spring:
  application:
    name: api

feign:
  client:
    config:
      default:
        errorDecoder: com.example.orchestrator.errordecoder.CountryErrorDecoder